# HOLES

仕様どおりに作れなかったところ、仕様が黙っていたところ、実装が先に決めてしまったところを記録する。
ここが上流（[stemcell-component-prompts](https://github.com/1101hirokin/stemcell-component-prompts)）へ
還流する種になる。

## #1 意味を持つ要素の選択（`as`）は Web の prop である


`Box.md` §4 が「意味要素の選択（`as` 相当）を中立に表現できるか。現状は各実装の表現に置いた」を
未確定にしていた。SwiftUI で書いてみて、答えが出た。

写せない。あれは HTML の要素を選ぶための prop であり、SwiftUI に要素の語彙が無い。
`section` にするか `nav` にするかという選択そのものが存在しない。

そして写す必要も無い。要素を選ぶ目的は意味を支援技術へ届けることで、SwiftUI ではそれを
到達性の修飾子が担う（`guidelines/accessibility.md` の写像表がまさにこの形を書いている。
役割に相手が居ないときは名前と値と状態で伝える）。

だから `as` は Web 固有の充足である。`Button.type` や `keyboard=email` と同じ側に置くのが
筋だと考える。中立契約から外し、Web の実装が持つ形。

## #2 交差軸の `stretch` は提案であって、子の箱を広げない


契約は `align: stretch`（既定）を「内容が交差軸方向にいっぱいに広がろうとする」と定める。
Web はこれを `align-items: stretch` で満たしていて、そこでは子の箱そのものが広がる。
子が自分で付けた背景も、広がった箱いっぱいに塗られる。

SwiftUI では同じことが起きない。`VStack` と `HStack` は、揃えが何であれ交差軸の寸法を
子へ提案する。そこから先は子の裁量で、提案を受ける子（`Color`、`TextField`）は広がり、
自分の理想で止まる子（`Text`、`Button`）は止まる。親から強制する口が無い。

iPad Pro 13（iOS 26.1）で両方向とも測った。

inline（交差軸は高さ）。背の高い子で行の高さを 44pt に決めた並びで、

| 子 | stretch | start | center | end |
|---|---|---|---|---|
| `Color`（受ける） | 44.0pt | 44.0pt | 44.0pt | 44.0pt |
| `Text`（受けない） | 上端 | 上端 | 中央 | 下端 |

stack（交差軸は幅）。幅 1000pt のコンテナで、

| 子 | stretch | start | center |
|---|---|---|---|
| `Color`（受ける） | 1000.0pt | 1000.0pt | 1000.0pt |
| `Text`（受けない） | 左端 | 左端 | 中央 |

方向によらず同じ形になる。受ける子は揃えに関わらず交差軸いっぱいになり、`align` では
縮まない。受けない子は `stretch` と `start` が同じ姿になる。

つまり表せているかどうかは子の側で割れる。受けない子に対しては三つを表せていて
`stretch` だけが `start` と重なる。受ける子に対しては四つとも表せていない。

`GeometryReader` の下では話がさらに変わる。高さ 200pt の `GeometryReader` に置くと、
受ける子でも交差軸いっぱいにならない。これは今回の直しの前後どちらでも同じで、
回帰ではないが、記録が無かったので書いておく。

`ForEach(subviews:)` で子を割って `.frame(maxWidth: .infinity)` を配る形も試したが、
上の数字は画素まで変わらなかった。枠を足しても子の描き方は変わらないためで、
効果を示せない機構は置かないことにした。ただし実験のコードは残していないので、
この比較は事後には再現できない。状況証拠と、提案の仕組みからの筋道である。

還したい問いは二つある。

一つ目。契約の `stretch` が要求しているのは箱の寸法か、それとも「広がってよい」という
許可か。Web は `align-items: stretch` で子の箱そのものを広げているので、実装から読めば
前者だと考える。

二つ目。前者だとして、SwiftUI が第7条の退避としてよいのはどこまでか。受けない子に対して
`stretch` の退避先が `start` と同じでよいか。受ける子に対して `start` が効かないことを
どう扱うか。Web はここを非対称にしていない。`align-items: flex-start` は子の種類を問わず
内容の寸法へ縮める。この非対称は SwiftUI の側にだけある。

## #3 `align` の四つは実機で見た（#2 へ畳んだ。番号は識別子なので欠番にしない）


`start` / `center` / `end` が Web の `align-items` と同じ位置に置かれることを確かめた。
`center` は行 88px に対して上から 24px（(88-40)/2）で、ちょうど中央だった。
測った数字は #2 の表に移した。この項目はここで閉じる。

## #4 当たり判定の床を、押せる範囲ではなく見た目へ当てている


`ButtonStyle` は `.frame(minWidth: 44, minHeight: 44)` を `.background()` より前に置いて
いる。だから面そのものが 44pt へ膨らむ。

iPad Pro 13 で測った。

| 段 | いまの高さ | 内側余白から逆算した本来の高さ |
|---|---|---|
| sm | 44.0pt | 36.5pt |
| md | 44.5pt | 44.5pt |
| lg | 52.5pt | 52.5pt |

本来の段は 36.5 / 44.5 / 52.5 という 8pt 刻みで、床が持ち上げているのは `sm` だけである。

これは契約が黙っているところではなく、契約に反しているところである。
`size.rules.json` の `minimumTargetSize` は `"appliesTo": "hit-region"` と定め、
註釈に「見た目の大きさではなく押せる範囲。したがって段と密度は見た目を詰めてよい。
層が違う」と書いている。押せる範囲へ当てるべきものを見た目へ当てた実装の誤りである。
`.frame` を `.background()` の後ろへ移し、`.contentShape(Rectangle())` を足して直した。
面を持たない強調度（outlined と text）は透明な地を持ち、SwiftUI は透明な画素を押せる範囲に
数えないので、字の上でしか反応していなかった。これも実機で見つかった。

Web も守れていない。`Disclosure` と `Tabs` と `Rating` は `min-block-size: 1.5rem` を
要素そのものへ当てている。24px が自然な高さを下回るので見た目に出ないだけである。
`appliesTo: hit-region` はどちらの土地でも実装されていない。上流へ還すのはこの形だと考える。

床の値そのものがトークンに無い件は残る。`size.md` §7 が「最低基準の値は foundations にも
`@stemcell/tokens` にも無い」を未確定としているところで、`44` を直に書いている。
トークンを引いていない唯一の数である。

## #5 揃えは子を縮められない


Web の `align-items: flex-start` は、交差軸で子を内容の寸法へ縮める。
SwiftUI の Stack は交差軸の寸法を子へ提案し続けるので、提案を受ける子は
`start` を指定しても縮まない（#2 の二つの表の一行目）。

`.fixedSize()` は子を理想の寸法で止めるが、これは CSS の `fit-content` ではなく
`max-content` に当たる。長い文が折り返さずに溢れる。写像として近くないので採らなかった。

伸びないことより縮まないことのほうが差として大きいと、自分は見ている。
ここも #2 の二つ目の問いに帰着する。

## #6 字の役の字送りが写せていない

`typography.md` §4 は役ごとに字送りを定めている。`label` の三つは `wide`（0.02em）、
`display` と `headline` は `tight`（-0.01em）である。`.textStyle()` はこれを当てていない。

原因はトークンの側にある。`stemcell/size/points` の変換は `px` で終わる値だけを数へ落とし、
それ以外はそのまま返す。字送りは `-0.01em` のような em 建てなので数にならず、Swift の
書式器が持つ数値の判定から外れて、出さないトークンの一覧へ落ちる。Base.swift の冒頭が
「土地の写像が要る」と記録しているのはこの結果である。

写せない値ではないと考える。行の高さは 1.334 という無次元の比のまま出ていて、SwiftUI 側は
それを `size * (lineHeight - 1)` で実寸へ開いている。字送りも em は比なので、同じ道が通せる。
`.tracking()` は pt を取るが、`size * em` で出せる。

要るのはトークンが比を出すことである。`em` も落として無次元で通すか、字送り専用の変換を
足すか。値が来れば SwiftUI 側は `.tracking()` へ繋ぐだけで済む。

いまは字送りが全役で 0 である。`label` と `display` が契約どおりに組めていない。

## #7 Dynamic Type の伸びの相手を、実装が独りで決めている

トークンは字の大きさを持つが、その字が利用者の設定でどう伸びるかは持たない。SwiftUI で
伸ばすには `@ScaledMetric` に native の役（`Font.TextStyle`）を渡す要があり、どの役へ
渡すかは契約のどこにも書いていない。実装が決めた。

規則は一つにした。トークンの大きさに一番近い native の役へ合わせる。同じ距離に二つ
並ぶときは大きいほうを採る。

最初は役の意図（display / headline / title …）で選んでいた。それだとトークンで大きさが
同じ役に別の伸び率が付く。`headlineMd` と `titleLg` はどちらも 21pt なのに、最大設定で
2.33 倍と 2.04 倍に割れていた。トークンに無い階層を Dynamic Type が作り出していたことになる。
大きさで選ぶ規則へ替えて、iPad Pro 13 で両方 44.0pt に揃うことを測った。

残る歪みが一つある。下端が潰れる。`labelSm`（10.5pt）と `labelMd`（12pt）は最大設定で
どちらも字の輪郭の幅が 44.5pt になり、見分けが付かない。iOS の `caption` と `caption2` が
アクセシビリティの側で寄るためである。iOS の作法としては正しい振る舞いだが、
トークンの段はそこで消える。

Web は rem 建てなので拡大が線形で、役の間の比はどの倍率でも変わらない。iOS は
Apple の曲線に乗るので、拡大するほど比が縮む。同じ契約から拡大の形が二つ出ている。
どちらかを直す話ではないと考える。契約が拡大時の比をどう扱うかを言っていないので、
そこへ一行足すのが筋だと思う。

## #8 読むだけの欄が焦点を受けられない

`state.md` §6 は ARIA を引いて `readonly` を「編集はできないが、それ以外は操作できる」と
定める。SwiftUI の `TextField` にその状態が無い。

使える口は三つあって、どれも要求を満たさない。

| 使える口 | 打てなくなるか | それ以外は操作できるか |
|---|---|---|
| `.disabled(true)` | なる | 焦点を受けられない。字も灰へ落ちる |
| `.allowsHitTesting(false)` | なる | 焦点を受けられない |
| 読むだけの束縛を渡す | なる | 満たす。ただし部品ではなく消費者が持つ |

三つ目だけが満たすが、それは prop ではなく束縛の作り方であり、部品が強制できない。
`readOnly: true` と書いても打ててしまう。

いまは一つ目を採り、中身だけを無効にして打てなくしている。`.disabled()` が字を灰へ
落とすぶんは `.foregroundStyle` で打ち消して、読むだけと使えないことの見分けは保った。
焦点を受けられないぶんは満たせないままである。

裁定を上流へ返した（RFC 0028）。契約が「打てない」だけを要求する形へ寄せる案を推している。

支援技術が読むだけの欄をどう扱うかは確かめていない。VoiceOver で焦点が行かない欄の中身を
拾えるかを測っていない。

## #9 hover と押下の時間を持っていなかった


`state.md` §3.3 は `hover` と `pressed` を面のチャンネルで解決し（`pressed > hover`）、
`focus` を輪のチャンネルで解決すると定める。トークンも役ごとに `bgHover` と `softBgHover`
を出していた。実装がどれも引いていなかった。

`ButtonStyle` は struct なので `@State` を置けない。中を View に分けて `onHover` を受ける
形にした。`Motion.Duration.fast02`（0.11 秒）で移す。動きを減らす設定のときは
`Motion.None`（0 秒）を引いて時間を持たない。

無効のときは相互作用の状態を出さない。§3.2 が「操作できないものにホバーの反応を返すことは
ユーザーへの嘘であり、第1条がこれを禁じる」と定め、放っておいて成立する事実ではないので
実装が明示的に満たす要がある、とまで書いている。

## #10 連続曲率の質感が土地で揃わない


`shape.md` §4-1 は「正準値は一つで、それを自分の尺度へ写す責任は各プラットフォームが持つ」
と定め、Web の尺度への写像として `superellipse(1.8)` を裁定している。裁定の註釈は
「換算式の一次資料は無いため、実機で見比べて選んだ（svelte 実装で確認）」である。
その時点で比べる相手は Svelte だけで、SwiftUI の実装は存在しなかった。

比べられるようになったので測った。半径 60pt の角を両方で描かせ、上端からの輪郭を画素で
取り出して突き合わせた。

```
SwiftUI .continuous  135, 116, 105, 98, 93, 88, 84, 80, 77, 74, 72, 69, 67 ...
半径 120px の真円    120, 105,  98, 93, 89, 86, 83, 80, 77, 74, 72, 70, 68 ...
```

Apple の連続曲率は角の端で公称の半径より外へ張り出し、途中で真円と交差してからわずかに
角ばる。CSS の `corner-shape` は `border-radius` が定める角の箱に収まるので、箱の外へは
出られない。族が違う。

CSS の八つの引数と突き合わせた結果（差の二乗和）。

| 引数 | 0.6 | 0.8 | 1.0 | 1.2 | 1.4 | 1.6 | 1.8 |
|---|---|---|---|---|---|---|---|
| 差 | 19966 | 4446 | 260 | 4460 | 14724 | 28215 | 43609 |

`.continuous` にいちばん近いのは `superellipse(1.0)`、つまり円弧である。契約が採っている
1.8 は試した中で最も遠い。裁定は方向ごと逆だった。

姿を土地の間で揃えるほうを採り、SwiftUI 側を Web の曲線へ寄せた。`.continuous` をやめ、
Lamé 指数 `2^1.8 = 3.482` の形を自前で描く。CSS が実際に描く曲線へ指数を当てはめると 3.53
が出たので、CSS の引数 k は Lamé 指数 `2^k` に写っている。差し替え後の実測は、差の二乗和が
43609 から 5 に、最大差が 20.5pt から 0.5pt になった。

代償が二つある。無償で得られていた `.continuous` を手放したので、Apple が曲線を変えても
追随しない。角の描画が 96 点のパスになるので、native の実装より遅い方向である（未測定）。

上流の裁定が要る。同じ節が「SwiftUI と Compose はこの値をそのまま描画に渡せる」と書いて
いるが、渡せない。渡すと Web と別の曲線になる。同じことが Compose にも当てはまるはずで、
`SmoothRoundedCornerShape` の 0.6f が CSS の 1.8 と同じ曲線を描く根拠はどこにも無い。

## #11 焦点の輪を platform へ譲った


`focus-ring.md` は自らを「ほぼ全て Normative であり、a11y の最低基準にあたる」と書き、
幅 2px と隙間 2px と intent ごとの色を定めている。SwiftUI では描けなかった。

`Button` の姿は `ButtonStyle` が持つ（DESIGN.md §2）。その中から焦点の有無を知る口が無い。
`@Environment(\.isFocused)` を試したが、焦点が当たっていても false のままだった。
`.focusable()` を足した場合も同じだった。macOS 26 で撮って確かめている。

| 試したもの | native の輪 | 様式の中の isFocused |
|---|---|---|
| `Button` を `.focused($x)` で焦点へ | 出る | false |
| 上に `.focusable()` を追加 | 出る | false |

`FocusState` は View が持つもので、`ButtonStyle` は struct なので置けない。様式の外へ出す形
（消費者が `.stemcellFocusRing($x)` を書く）と、`Button` を型で出す形も考えたが、前者は
二つの道を作り、後者は §2 の判断そのものを覆す。

platform の輪へ譲った。第2条の Ceded に Liquid Glass と同じ形で載る。譲る先は無償で正しく
動き、システムのコントラスト設定やアクセントカラーにも追随する。捨てたのはトークンの色と
幅と隙間で、Apple の土地では `color.semantic.<intent>.focus-ring` が使われなくなる。

上流の裁定が要る。`focus-ring.md` は Ceded を想定していない。Normative のまま残すなら、
SwiftUI がどう満たすのかを誰かが示す要がある。私は道を見つけられなかった。

## #12 一行の字の高さが土地で揃わない


`typography.md` §3.2 は行の高さを固定比 1.334 で定める。CSS の `line-height` は一行でも
行 box の高さを決めるが、SwiftUI の `lineSpacing` は行と行のあいだにしか効かない。
一行の `Text` はその土地の自然な行高のままになる。

Web と並べて見つけた。md のボタンで字の箱が 16.0pt しか無く、`14 × 1.334 = 18.68` に
なっていなかった。`.textStyle()` の側で、足りないぶんを行のあいだと上下の余白へ分けて
配る形にした。自然な行高は `UIFont` / `NSFont` から取る。

もう一つ、Web の Button は `border: 1px solid transparent` を全段に置いて、outlined と
filled で箱を揃えている。SwiftUI の `strokeBorder` は重ねなので箱を増やさず、上下で 2pt
低くなっていた。枠の太さぶんを箱へ足した。

二つを直した後の段の高さ。

| 段 | Web（計算） | SwiftUI（実測） |
|---|---|---|
| sm | 36.68 | 36.0 |
| md | 44.68 | 44.0 |
| lg | 52.68 | 53.0 |

残る 1px 未満は、Web が 18.676px という端数を持つのに対し SwiftUI が画素へ丸めるためで、
これ以上は詰められない。

## #13 veil と影の不透明度が Swift のトークンから落ちている


Web は `--scrim: rgba(41, 47, 54, 0.4)` を出しているのに、`0.0.0-alpha.16` の Swift 出力は
同じ色を不透明のまま出す。影の二色も同じである。

不透明度は別名の先ではなく、その名前が `$extensions.stemcell.alpha` で持つ。`scrim` の値は
`{color.app.scrim}` という別名なので、値変換が走る時点では解決されておらず素通しする。
出力に載るのは解決後の gray.900 で、そこに alpha は無い。

直しは stemcell-tokens の PR #24 にある。版がまだ切られていないので、それまでテーマの側で
値を当てている。`elevation.md` §6 が「基底色を自前で透過させ直さない」と定めているところ
なので、版が上がったら消す。

## #14 焦点の出入りを実機で確かめていない


`Dialog` は焦点の移動と捕捉と復帰を native の提示へ委ねている。`overlay.md` §4 が
「捕捉の実現は表現である」と書いていて、提示の機構が持つはずだからである。

確かめていない。VoiceOver で見出しが先に読まれるか、巡回が札の中で閉じるか、閉じたときに
開いた元へ戻るか、どれも見ていない。`overlay.md` §7 自身が「SwiftUI と Compose で焦点の
移動と復帰が実機でどこまで無償かは未検証」を TODO に残していて、DESIGN.md §10 は `Dialog` を
それに答える部品として置いている。答えるべきところを黙って通過している。

自動で測る道具が無いのが理由である。触れる仕草を送る口も、読み上げを取り出す口も持って
いない。実機で人が見る要がある。

## #15 Liquid Glass を手放した


`Dialog` は `.presentationBackground(.clear)` で native の提示の地を消し、面と角と影を
自前で描いている。iOS 26 の既定はそこに Liquid Glass が乗る。

DESIGN.md §4 は「自前で面を描かず SwiftUI の部品に乗る、という §2 の判断がそのまま効く。
ここは楽である」と書いている。楽ではなかった。契約はscrim を要求していて（`tokensRequired` の
`scrim`）、提示の既定の地ではscrim の色を指定できない。scrim を捨てるか、ガラスを捨てるかの二択で、
scrim を採った。

第2条は「無償で得られるものを自前で再発明しない」と定めるので、これは例外にあたる。
例外にした理由を書いておく。ガラスの上にscrim を敷く道があるなら、そちらへ戻したい。

## #16 暗いテーマでscrim が効かない


`--scrim` は明暗どちらでも `rgba(41, 47, 54, 0.4)` で、暗いテーマの地 `#292f36` と同じ色で
ある。同じ色を重ねても暗くならない。iPad Pro 13 で測って、modal を開く前と後で背後の地が
どちらも `292F36` のままであることを確かめた。明るいテーマでは `FFFFFF` から `A9ACAF` へ
落ちるので、scrim の仕組みそのものは働いている。

これは SwiftUI の問題ではない。Web でも同じ値が出るので、暗いテーマでは同じことが起きる。

`elevation.md` §6 はscrim の目的を「背後を減光して手前の modal へ注意を集めつつ、元の文脈を
見失わない程度に透かす」と書いている。暗いテーマでその目的が果たせていない。基底色を
自前で透過させ直すことは同じ節が禁じているので、実装では直さない。上流の裁定を待つ。

## #17 `Popover` の焦点の出入りも実機で確かめていない


`Dialog` と同じ形の未検証である（#14）。`Popover` は焦点を中へ入れること、捕まえないこと、
閉じたらアンカーへ戻すことを native の `.popover` へ委ねている。`overlay.rules.json` の
`focus.interactiveEntersOrVirtual` がそれにあたる。

VoiceOver で見ていない。`overlay.md` §7 は popover 類へ modal 類より弱い保証しか置いて
いないので、断定は modal のとき以上に危うい。

## #18 ガラスの上のガラスという問いが消えた


DESIGN.md §4 は「ガラスはガラスを透かせないので、`Popover` を `Dialog` の上に出したときに
何が起きるか」を実機で見ると書いていた。その問いは前提ごと無くなった。

`Dialog` も `Popover` も `.presentationBackground(.clear)` でガラスを手放しているためである
（#15 に `Dialog` の理由がある。`Popover` は縁を当てられないという同じ形の理由による）。
ガラスが二枚とも無いので、透かせるかどうかを問う相手が居ない。

重ねたときの姿は見た。`Dialog` の中から `Popover` を出すと、層は正しく `Popover` が前に
出て読める。scrim も二重にならない（popover 類は `overlay.rules.json` の `scrim:false` で
scrim を敷かない）。ただしこれは §4 が恐れていたものを確かめたのではなく、別の問いに答えただけ
である。DESIGN.md §4 はいずれ書き直す要がある。

## #19 macOS の modal は位置とscrim を platform へ譲った


契約は modal を「ビューポート中央」に置き、背後へscrim を敷けと定める（`overlay.md` §5、
`Dialog` の tokensRequired の `scrim`）。macOS で満たせない。

`fullScreenCover` は iOS だけの API で、macOS には画面全体を取る提示が無い。残るのは
`sheet` で、あれは窓の上端から降りてくる。

一度は契約どおりに描こうとして、シートの中へscrim を敷いた。`ignoresSafeArea` の「いっぱい」が
シートの中で止まるので、scrim の箱の中に札の箱が入る二重の箱になった。窓ではなくシートの寸法で
scrim が切れる。撮って捨てた。

位置とscrim を platform へ譲った。面と角と影もシートが持つので描かない。書くのは見出しと中身と
脚の並びと、字の役だけである。焦点の捕捉と Escape は無償のまま残る。

譲って失ったものは、当初の見立てより小さかった。macOS はシートを出すあいだ、背後を自分で
減光する。`filled` ボタンの色を測ると、覆いを出す前が `1C42FF`、出した後が `7480CE` である。
`elevation.md` §6 がscrim に与えていた役目（背後を減光して手前へ注意を集めつつ、文脈は
見失わせない）は、この土地では OS が果たしている。失ったのはscrim そのものではなく、scrim の色を
トークンで決める権利だけである。

裁定が要る。焦点の輪（RFC 0026）と同じ形で、`overlay.md` §5 の位置とscrim を Apple の土地で
Ceded へ落とすかどうかである。

シートの位置は窓の上端ではなく三分の一ほど下に出る。中身が小さいときの SwiftUI の置き方だと
思うが、確かめていない。

## #20 明るいテーマで、影の面が地から見分けられない


`Card` の既定は影の面である。明るいテーマで地の上に置くと、境界がほとんど出ない。

iPad Pro 13 で測った。カードの上端と左端は地と同じ `FFFFFF` で境界が無い。影が乗る下端と
右端でも最も濃いところが `FBFBFB` で、差は 4/255（比で約 1.02:1）である。

実装の欠陥ではなく、規範の帰結である。`elevation.md` §8 が明るいテーマで `surface` と
`surface-raised` と `overlay` を `background` へ潰すと定め、§4 が光源を真下に置く
（`offsetX` が 0）と定めている。面色で分けられないうえ、影は下と右にしか出ない。

浮かぶ面は §3 の裁定（2026-07-26）で縁を必須にして救われている。`surface` 層の `Card` に
その救済が無い。`Card.md` の未確定は暗いテーマの件だけを書いていて、明るいほうは書いていない。

`outlined: true` にすれば明暗どちらでも境界が出る。既定を枠の面にするか、明るいテーマの
`surface` を地から少し離すか、影を強くするか。上流の裁定が要る。

## #21 面を入れ子にすると段が付かない


`Card` の中に `Card` を置くと、外と内が同じ面色になる。契約は入れ子を禁じていない。

測った。明るいテーマは外も内も `FFFFFF` で完全に同色、境界の陰影差が最大 4/255。
暗いテーマは外も内も `38414B` で同じ十六進値、境界の差は 1/255（`37404A` 程度）である。
明るいほうより弱い。

`elevation.md` §5 の表が `surface` と `navigation` を同じ面へ割り当てている帰結である。
`Card` は `surface` の段なので、入れ子にしても段が上がらない。

内側を `outlined: true` にすれば明暗どちらでも境界が出ることは確かめた。`Card.md` に
運用の注記を足す価値があると考える。動くが読めない状態を黙って通すのは、同じ文書が
暗いテーマの件を未確定として書いている姿勢と揃わない。

## #22 折り返す並びの RTL は無償で正しかった


`Cluster.md` §4 は「RTL と縦書きでの折返し方向は論理プロパティが無償で解くはず。実装で確認」を
未確定にしていた。横方向は確認できた。

`.environment(\.layoutDirection, .rightToLeft)` を当てて iPad Pro 13 で見たところ、要素の
並びが右起点へ反転し、折り返しの詰め方向も反転した。`Layout` の `layoutDirectionBehavior` は
既定が `.mirrors` で、自前で反転を書いていない。書けば既定と二重に効いて壊れていた。
何もしないのが正しかった。

縦書きは確かめていない。SwiftUI に相当する汎用の機構が見当たらないので、`Cluster.md` の
未確定はそちらだけ残る。

## #23 密度がどこからも読まれていない


`.stemcellDensity(_:)` を public に出しているが、`Spacing` も `Stack` も `Cluster` も
`\.stemcellDensity` を参照していない。呼んでも何も起きない口である。

`spacing.md` §5 は SwiftUI について「無し（Ceded）。`controlSize` と Dynamic Type に従う」と
書いている。だとすると独自の密度の軸を public に出していること自体が規範とぶつかる。

どちらかである。口を消して `controlSize` へ寄せるか、トークンの compact の出力を引いて
`resolveSpacing` の段を差し替えるか。前者が §5 に沿うと自分は見ている。裁定が要る。

## #24 読むだけの欄を面の色で示しているが、その色は明るいテーマで地と同じである

`FieldStyle` は読むだけの欄を「枠は残し、面だけを地へ寄せて打てないと見せる」形にしている。
これが明るいテーマで効かない。

トークンの元を読むと、明るいテーマの `color.app.surface` と `surface-raised` は
`color.app.background` の別名である。地も面も持ち上げた面も同じ `#FFFFFF` で、明るい側の
段は色ではなく影が持っている。面を地へ寄せる操作は、寄せる先と同じ色なので空振りする。
撮って測っても、読むだけの枠と普通の枠はどちらも `FFFEFF` だった。

もっと重いのは、この見せ方が SwiftUI の発明だということである。svelte の `TextField` は
`readonly` に視覚の差を一切与えていない。native の属性として渡すだけで、姿は普通の欄の
ままである。契約も `state.md` §6 が「readonly は ARIA が property と定義する」と書くだけで、
どう見せるかは持っていない。

`OneTimeCodeField` は svelte に揃えて面を動かさないことにした。効かない差を真似ても
仕方がない。`FieldStyle` のほうは直していない。読むだけを視覚で示すか示さないかは
両方の土地に関わるので、上流の裁定を仰ぐ（RFC 0030）。

## #25 桁ごとの焦点の輪と、native が並び全体へ描く輪が二重になりうる

`OneTimeCodeField` は、いま打っている桁に `focus-ring` のトークンで輪を描く。契約が
`color.semantic.plain.focus-ring` と `danger.focus-ring` を名指し、svelte も桁ごとに
出しているためである。「焦点の輪は platform へ譲る」とした HOLES #11 とは事情が違う。
あちらは `ButtonStyle` の中から焦点を知る道が無かったが、ここは `@FocusState` が効く。

問題は、本物の欄が一つで、それが枠の並び全体に広がっていることである。native が焦点の輪を
描くなら、それは並び全体を囲む形になる。桁ごとの輪と重なって二重に見えるはずだが、
確かめていない。押した後の姿は画面を出さずに焼く道具では撮れない（DESIGN.md §7.1）。

macOS で `.textFieldStyle(.plain)` の欄が焦点の輪を持つかどうかも見ていない。二重に
なるなら、桁の輪を残して native の輪を消すか、その逆かを決めることになる。

## #26 段を変えても押せる範囲の床が動かない

枠の高さは行高（打つ字の一行と縦の inset 二つ分）で決まり、段で変わる。md で 43.0pt を
測った。svelte の `internal/field-button.css` が「md で 43px」と書いている値と一致する。

一方、押せる範囲の床は 44pt で固定である（size.rules.json の iOS の値）。sm の枠は
38.4pt になるので、見える枠より押せる範囲のほうが広い。段を下げても押しやすさは
下がらないので体験としては正しいが、見えるものと押せるものの寸法が段によって割れる。

svelte は 24px（WCAG 2.2 SC 2.5.8）を門にしているので、sm でも枠のほうが広く、割れない。
iOS の 44 と Web の 24 で門の高さが違うことが、そのまま表に出た形である。
size.md がこの差をどう扱うかを書いていない。
## #27 落とす面について、触らないと分からないことが三つ残っている

`DropArea` は `dropDestination` に乗せた。画面を出さずに焼く道具では常のときと使えない
ときの姿しか撮れないので、実際に何かを引いてきたときのことが確かめられていない。

一つ目。落ちてくる最中に `isTargeted` が期待どおり働くか。ドロップ先はビューの枠への
ヒットテストで決まるので、Web のように子の上で出入りが飛ぶことは無いはずだと考えて
深さを数えていない。中へ別の `dropDestination` を持つ子を置いたときも見ていない。

二つ目。`.disabled()` が native の側でドロップそのものを止めるかどうか。止めるなら、
実装が置いている番人（`guard isEnabled`）は通らない。止めないなら、macOS でカーソルが
「落とせる」形のまま面の色だけが「落とせない」姿になる。二重でも害は無いので番人は
置いてあるが、どちらなのかは知らない。

三つ目。落ちてきた URL がセキュリティスコープ付きかどうか。`fileImporter` から得た URL は
`startAccessingSecurityScopedResource` の対で囲むのが Apple の作法だが、`dropDestination`
から来たものに同じ作法が要るかを確かめていない。要るなら受け取った側の仕事になる。面は
値を持たないので、開いて読むのは常にアプリである。型の註にその旨を書いた。

XCUITest を組むか、人に引いてもらうかのどちらかが要る。

## #28 生きた領域にあたる機構が無い

契約は `FileField` について「落とす・貼る経路で受け取った件数と弾いた件数は、常設の領域で
知らせる。割り込ませない」と定めている（WCAG 2.2 SC 4.1.3）。svelte は `role="status"` の
領域を置き、視覚からだけ隠している。

SwiftUI に相当する機構が無い。`AccessibilityNotification.Announcement` はあるが、あれは
割り込むもので、契約が名指しで避けている形である。視覚から隠したまま支援技術にだけ届ける
道も見当たらない。`.accessibilityHidden(false)` と大きさ 0 の組み合わせは、隠すと同時に
辿れなくもする。

いまは見える文にしている。常設であることは満たすが、目で見る側にも出るので svelte と姿が
割れる。件数は目で見る側にも役に立つ情報なので悪い形ではないと考えているが、揃っていない
ことに変わりはない。裁定が要る。

## #29 貼り付けの口は姿も文言も譲るしかない

`FileField` の貼り付けは `PasteButton` に乗せた。契約の a11y notes が「iOS は貼り付けボタン
（`UIPasteControl`）を置く」と名指していて、`PasteButton` の実体がそれである。押したときだけ
貼り付け先へ触れるので、環境が貼り付けの確認を出さない。

代わりに姿を作れない。`PasteButton` は `buttonStyle` を受け付けず、文言も自分で持つ。それが
privacy の保証と引き換えだからである。渡せるのは `controlSize` と `tint` と
`buttonBorderShape` だけで、渡した名前は読み上げにしか効かない。撮ると、選ぶボタンが
stemcell の姿で並ぶ隣に、環境の姿の「Paste」が立つ。

文言を環境が持つのは i18n.md §1（DS は文言を持たない）とはむしろ揃う。揃わないのは姿の
ほうで、`variant` も `size` の段も効かない。Ceded の一覧へ足すのが筋だと考える。
